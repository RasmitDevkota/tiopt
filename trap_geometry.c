#include "trap_geometry.h"

#include <stdio.h>

#include <gmshc.h>

// generate trap from input parameter array
void generate_trap_from_parameters(
    struct Trap *trap,
    const double *x
) {
    printf("Not implemented!\n");
}

// generate the initial mesh object needed from the trap electrodes
void generate_mesh(
    struct Trap *trap
) {
    printf("Not implemented!\n");
}

